# product-categorizer

Uses your local Google Cloud SDK credentials (Application Default Credentials) and by default runs against the `prochazka-ml-playground` project.

You can override the project at runtime with the `--project` flag or by setting the `GOOGLE_CLOUD_PROJECT` environment variable.